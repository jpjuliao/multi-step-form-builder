<?xml version="1.0" encoding="UTF-8"?>
<ruleset name="Multi Step Form Builder">
    <description>Basic coding standards for Multi Step Form Builder plugin</description>

    <!-- Basic PSR-12 standards with exclusions for WordPress compatibility -->
    <rule ref="PSR12">
        <exclude name="PSR1.Methods.CamelCapsMethodName.NotCamelCaps"/>
        <exclude name="Squiz.Classes.ValidClassName.NotCamelCaps"/>
        <exclude name="PSR1.Files.SideEffects.FoundWithSymbols"/>
        <exclude name="Generic.Files.LineLength.TooLong"/>
    </rule>
    
    <!-- Exclude vendor and tests directories -->
    <exclude-pattern>vendor/</exclude-pattern>
    <exclude-pattern>tests/</exclude-pattern>
    <exclude-pattern>node_modules/</exclude-pattern>
    
    <!-- Include all PHP files -->
    <file>.</file>
    
    <!-- Show progress -->
    <arg value="p"/>
    
    <!-- Set colors -->
    <arg name="colors"/>
    
    <!-- Show sniff codes in all reports -->
    <arg value="s"/>
    
    <!-- File extensions to check -->
    <arg name="extensions" value="php"/>
    
    <!-- Maximum line length -->
    <rule ref="Generic.Files.LineLength">
        <properties>
            <property name="lineLimit" value="200"/>
            <property name="absoluteLineLimit" value="250"/>
        </properties>
    </rule>
</ruleset>
